{
  "name": "red-portal",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/copy-uv.js",
    "dev": "npm run copy-uv && vite",
    "build": "npm run copy-uv && vite build",
    "preview": "vite preview",
    "copy-uv": "node scripts/copy-uv.js"
  },
  "dependencies": {
    "@titaniumnetwork-group/ultraviolet": "^3.2.4",
    "@mercuryworkshop/bare-mux": "^2.0.0",
    "@tomphttp/bare-server-node": "^2.0.3"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
